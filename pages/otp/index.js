import PhoneAuth from "@/components/auth/PhoneAuth";
import React from "react";

const index = () => {
  return (
    <div className="flex h-screen justify-center items-center backdrop-blur-sm backdrop-brightness-50">
      <PhoneAuth />
    </div>
  );
};

export default index;
